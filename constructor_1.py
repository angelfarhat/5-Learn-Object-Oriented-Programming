class Wall:
    def __init__(self, depth, height, width):
        self.depth = depth
        self.height = height
        self.width = width 
        self.volume = self.volume = self.depth * self.height * self.width
     
wall = Wall(2,3,4) #need this information to get data 
